# AGENTS.md

## 適用範囲
- このファイルには、このプロジェクト固有のルールのみを書く。

## 追加ルール

### 移植方針
- やねうら王 C++版と原則 1:1 になるように実装する。
- やねうら王 C++版と同じ挙動になるように実装する。
- やねうら王を `YANEURAOU_ENGINE_NNUE` としてビルドした際の挙動に合わせる。

### 作業ミスの再発防止（プロジェクト固有）
#### 計画参照
- 実装開始前に、参照する計画ファイルを1つ主対象として明示する。
- 実装後に計画との差分が出た場合は、計画ファイル側の進捗欄を同時更新する。

#### USIプロトコル安全性
- `stop` 応答は「思考中のみ `bestmove` を返す」を維持し、重複 `bestmove` を発生させない。
- `bestmove` 送信前に合法手であることを必ず最終確認する。
- `go infinite` / `go ponder` を含む全探索経路で、`Threads` 設定が一貫して反映されることを維持する。
